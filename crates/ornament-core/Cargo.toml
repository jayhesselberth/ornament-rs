[package]
name = "ornament-core"
description = "Modification-aware tRNA analysis library"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[features]
# Propagate GPU-accelerated filtering down to the scan engine (see ornament-scfg's `cuda`).
cuda = ["ornament-scfg/cuda"]

[dependencies]
# Native Rust covariance-model search engine.
ornament-alphabet = { workspace = true }
ornament-scfg = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
rustc-hash = { workspace = true }
rayon = { workspace = true }
lazy_static = { workspace = true }
regex = { workspace = true }
bio = { workspace = true }
# Log facade only — status warnings are emitted as tracing events; the binary
# (ornament-cli) installs the subscriber that renders them.
tracing = { workspace = true }

[dev-dependencies]
